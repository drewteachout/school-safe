import React from 'react';
import { View, Text, Button } from 'react-native';
import { styles } from './config';

export function AdminScreen() {
	return (
		<View style={styles.container}>
			<Text>Admin Page Currently Not Available</Text>
		</View>
	);
}